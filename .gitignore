# Terraform / OpenTofu state and cache
.terraform/
.terraform.lock.hcl
terraform.tfstate
terraform.tfstate.backup
crash.log

# Variable files with secrets — use terraform.tfvars.example as template
terraform.tfvars
*.auto.tfvars

# Override files
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Sensitive outputs
kubeconfig
talosconfig
*.kubeconfig
*.talosconfig

# Local environment directories (personal test environments)
environments/
